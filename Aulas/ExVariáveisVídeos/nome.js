let nome = 'Julio';

let sobrenome = 'Matias';

let nomeCompleto = nome + sobrenome; // + junta, os espaços são ignorados;

nomeCompleto = nome + " " + sobrenome; // as " " geram um espaço entre as duas variáveis;

console.log(nomeCompleto);