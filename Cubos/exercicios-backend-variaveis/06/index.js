//Calcular a velocidade média dessa pessoa a partir da distância percorrida pela pessoa e pelo tempo que passou, usando a fórmula abaixo:

// Velocidade = distPercorrida / tempo

let distPercorrida = 12, tempo = 08, velocidade = (distPercorrida / tempo) * 3.6;

console.log(` A sua velocidade média é ${velocidade} km/h `);
